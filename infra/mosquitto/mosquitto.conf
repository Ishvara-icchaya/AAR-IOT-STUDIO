# Dev / lab — replace with password_file + deny anonymous for production.
listener 1883
protocol mqtt

allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_timestamp true
