import { DashboardResolvedPage } from "@/pages/dashboard/DashboardResolvedPage";

/** Enterprise entry — same resolved primary / default payload as `/dashboard`. */
export function EnterpriseDashboardPage() {
  return <DashboardResolvedPage toolbarVariant="enterprise" />;
}
