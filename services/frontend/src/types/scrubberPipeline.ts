/** Scrubber Studio guided pipeline step ids (shared by page + help UI). */
export type PipelineStepId =
  | "drop"
  | "addAttributes"
  | "scalars"
  | "decodeSeries"
  | "functionBased"
  | "gps"
  | "health"
  | "kpi";
